# Database Connection
# SQLite (Default for local development)
DATABASE_URL="file:./dev.db"

# PostgreSQL (Production / Custom Database)
# DATABASE_URL="postgresql://username:password@localhost:5432/acresbazaar_db?schema=public"

# JWT Configuration
JWT_SECRET="your_jwt_secret_key_change_in_production"
JWT_EXPIRES_IN="7d"

# Server Port
PORT=5001
